package com.example.interfaces;

// An interface declares a contract that implementing classes need to follow.
// A class can implement any number of interfaces.
public interface Interfaces {

    // All interface methods are public by default and have no body
    void doSomething();
}