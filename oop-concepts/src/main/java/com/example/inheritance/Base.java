package com.example.inheritance;

// Classes can inherit from a single parent, and many classes can have
// the same parent, all classes that don't explicitly inherit from a
// parent inherit from java.lang.Object.
public class Base {}